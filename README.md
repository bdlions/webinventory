<pre>
---------Inventory software in online---------

---------------password generator------------
generating htpasswd by xampp apache/bin/htpasswd.exe
openning command prompt and going to the bin directory write : 
htpasswd.exe -c -b .htpasswd alamgir pass
----------------------------------------------

For security purpose
1.We will hide our controller name(It will more readable and decrease url length if we hide it)
2.We will hide function name also
3.We will not use parameter based controller function instead of parameter we will use query string
    Example(http://webinventory/product.php?productId=12 instead of http://webinventory/product/12)

<pre>


